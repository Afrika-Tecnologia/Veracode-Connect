name: Security Scan (Auto Packager + Sem Baseline + SCA + IaC + Upload & Scan)

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Pipeline Scan + SCA + IaC + Upload & Scan sem Baseline (Auto Packager)
        uses: Afrika-Tecnologia/Veracode-Connect@v1
        with:
          veracode_api_id: ${{ secrets.VERACODE_API_ID }}
          veracode_api_key: ${{ secrets.VERACODE_API_KEY }}
          enable_auto_packager: 'true'
          baseline_mode: 'none'
          enable_pipelinescan: 'true'
          enable_upload_scan: 'true'
          veracode_sandbox: 'true'
          veracode_sandbox_name: 'dev'
          policy_fail: 'false'
          enable_sca: 'true'
          veracode_sca_token: ${{ secrets.VERACODE_SCA_TOKEN }}
          enable_iac: 'true'
