name: Security Scan (Pipeline Desativado + Upload & Scan Apenas - Auto Packager)

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Upload & Scan Apenas (Pipeline Desativado - Auto Packager)
        uses: Afrika-Tecnologia/Veracode-Connect@v1
        with:
          veracode_api_id: ${{ secrets.VERACODE_API_ID }}
          veracode_api_key: ${{ secrets.VERACODE_API_KEY }}
          enable_pipelinescan: 'false'
          baseline_mode: 'none'
          enable_upload_scan: 'true'
          veracode_sandbox: 'true'
          enable_auto_packager: 'true'
